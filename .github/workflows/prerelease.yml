# file: .github/workflows/prerelease.yml
# version: 1.0.0
# guid: 9f8e7d6c-5b4a-3c2d-1e0f-9a8b7c6d5e4f

name: Prerelease on Merge

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.github/workflows/nightly.yml'
  workflow_dispatch: {}

concurrency:
  group: prerelease-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

jobs:
  prerelease:
    name: Build + publish burndown binary
    uses: falkcorp/github-common/.github/workflows/reusable-release.yml@bfb912c8e63e7f8e33df9c20c5c25c04083ec0b4 # v1.10.7
    with:
      release-type: 'auto'
      prerelease: true
      draft: false
      go-enabled: true
      docker-enabled: false
      build-target: 'go'
    secrets: inherit
